# JustLayMe Environment Configuration

# Google OAuth (required for Google Sign-In)
GOOGLE_CLIENT_ID=your-google-oauth-client-id

# Email Configuration (required for email verification)
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-gmail-app-password

# Database (optional - uses SQLite by default)
DATABASE_URL=postgresql://user:password@localhost/justlayme

# Server Configuration
PORT=3000
JWT_SECRET=justlayme-secret-key-change-this

# Ollama Configuration
OLLAMA_URL=http://localhost:11434
AI_MODEL=zephyr:7b-alpha-q4_0

# Optional: GPU Load Balancer
USE_GPU_LOAD_BALANCER=false
LOAD_BALANCER_URL=http://localhost:3000

# Optional: Stripe for payments
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key